apiVersion: apps/v1
kind: Deployment
metadata:
  name: madeleine
  namespace: madeleine
spec:
  replicas: 1
  selector:
    matchLabels:
      app: madeleine
  template:
    metadata:
      labels:
        app: madeleine
    spec:
      containers:
        - name: app
          image: ghcr.io/mschmidlin1/madeleine-portfolio:latest
          ports:
            - containerPort: 80
          livenessProbe:
            httpGet:
              path: /
              port: 80
            initialDelaySeconds: 5
            periodSeconds: 10
          readinessProbe:
            httpGet:
              path: /
              port: 80
            initialDelaySeconds: 3
            periodSeconds: 5
          resources:
            requests:
              cpu: 50m
              memory: 64Mi
            limits:
              cpu: 250m
              memory: 128Mi
